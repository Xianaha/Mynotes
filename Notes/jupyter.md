# jupyter

## 快捷键

`Ctrl + Enter` 运行当前单元格
`Shift + Enter` 运行当前单元格并选择下一单元格
`Alt + Enter` 运行当前单元格并创建新的一行
`Ctrl + Shift + -` 减少当前单元格的缩进
`Ctrl + Shift + +` 增加当前单元格的缩进

## 关键字
`!` 运行shell命令
```jupyter
# 展示当前目录下的文件列表
! ls
```
